# Copy to .env. The service, the ingest CLI and the MCP server all read it,
# and all three refuse to start without the key.
OPENAI_API_KEY=

QDRANT_URL=http://localhost:6333
HINDSIGHT_SERVICE_URL=http://127.0.0.1:8000
