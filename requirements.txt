# Pinned loosely: hackathon, prefer working over reproducible.
qdrant-client>=1.15,<1.16  # keep in step with the server image in docker-compose.yml
fastembed>=0.4
openai>=1.54
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27
python-dotenv>=1.0
mcp>=2.2  # 2.x renamed FastMCP -> MCPServer; server.py targets the 2.x API
pytest>=8.0
